# Assets/

Standard UWP tile/splash images (to be generated by Visual Studio's asset generator once
opened there), plus optionally `Fonts/` with a custom monospaced TTF if we want a "retro
terminal" identity beyond the default Consolas/Cascadia Mono (see docs/UI-THEMING.md,
"Fonte customizada" section). Not required for MVP.
